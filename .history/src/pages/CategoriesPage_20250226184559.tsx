import { useGetCategoriesQuery } from '@/shared/api/categories'

export const CategoriesPage = () => {
    const [data,setData]

    return (
      <>
      <h1>Categories</h1>

      </>
    );
  };