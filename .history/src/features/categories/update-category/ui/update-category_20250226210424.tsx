import { IUpdateCategory } from "../model"
import { Button } from "@/shared/ui/button"

export const UpdateCategory = ({updateCategoryI}) => {
    return(
        <Button variant="outline">{updateCategoryId}</Button>
    )
}